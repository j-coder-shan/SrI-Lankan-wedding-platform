
spring:
  application:
    name: review-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

server:
  port: 8092 # A unique port for the review service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    instance-id: ${spring.application.name}:${instanceId:${random.value}}